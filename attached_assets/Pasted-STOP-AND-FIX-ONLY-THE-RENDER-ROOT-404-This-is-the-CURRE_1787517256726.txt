STOP AND FIX ONLY THE RENDER ROOT 404.

This is the CURRENT Ayush Medico project:
https://github.com/Nasty-Developer/Ayush-Medicozip

We are deploying the ENTIRE repository as ONE Render Web Service:
frontend + Express API + PostgreSQL.

Render deployment currently succeeds and the service is live:

https://ayush-medicozip-4.onrender.com

BUT Render logs show:

GET / → 404

The API server starts successfully, but the compiled Ayush Medico frontend is NOT being served at the root route.

DO NOT rebuild the application.
DO NOT change features.
DO NOT change the database.
DO NOT re-import medicines.
DO NOT create mock data.
DO NOT create another service.

Fix ONLY the deployment/server integration.

TASK:

1. Inspect the current Express API entrypoint.

2. Inspect where the Vite frontend is built.

3. Confirm the actual production frontend output directory.

4. Make the existing Express server serve that compiled frontend directory.

5. Configure:

GET /
→ frontend index.html

6. Configure SPA fallback so these routes also return the frontend:

/medicines
/cart
/checkout
/about
/contact
/categories
and other client-side routes.

7. Keep API routes untouched:

/api/*

They must continue going to the Express API.

8. Make sure the server uses Render's:

process.env.PORT

and does NOT hardcode a development port.

9. Verify locally using the PRODUCTION BUILD, not the development server.

Run the actual production build and start the combined server.

Then test:

GET /
GET /medicines
GET /cart
GET /checkout
GET /api/healthz

Expected:

/
→ HTTP 200 + Ayush Medico frontend

/medicines
→ HTTP 200 + frontend

/cart
→ HTTP 200 + frontend

/checkout
→ HTTP 200 + frontend

/api/healthz
→ HTTP 200 + {"status":"ok"}

10. If the frontend output path is different from what the current deployment adapter expects, use the REAL generated path. Do not guess.

11. Check the Render Build Command and Start Command are compatible with the resulting output.

Current intended commands are:

Build:
pnpm install --frozen-lockfile && pnpm --filter @workspace/ayush-medico run build && pnpm --filter @workspace/api-server run build

Start:
pnpm --filter @workspace/api-server run start

12. Do NOT remove the existing API.

13. Do NOT move the project into Vercel.

14. Do NOT create a separate frontend Render service.

15. Do NOT change PostgreSQL configuration.

16. After fixing the root 404, run the production build again.

17. Verify the combined production server again.

18. If everything passes, commit ONLY this deployment fix.

Commit message:

fix: serve frontend from combined Render server

19. Push to the CURRENT repository:

https://github.com/Nasty-Developer/Ayush-Medicozip

branch:
main

DO NOT force push.

FINAL REPORT MUST SHOW:

Frontend output directory:
Express frontend serving:
SPA fallback:
API health:
Root / status:
Medicines route status:
Cart route status:
Checkout route status:
Production build:
Commit hash:
Push status:
Any remaining blocker:

IMPORTANT:
Do not say the deployment is fixed until GET / actually returns HTTP 200 from the production-style combined server.