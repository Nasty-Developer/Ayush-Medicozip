IMPORTANT: Fix the complete PRODUCT ORDERING and CHECKOUT experience.

The current website has inconsistent ordering behavior and the order flow sometimes feels slow/laggy even for simple actions.

I want you to inspect the entire product-ordering system and make it reliable, fast, and consistent.

==================================================
1. EVERY PRODUCT MUST HAVE A WORKING ACTION
==================================================

Check ALL product categories/types, including:

- General medicines
- Prescription medicines
- Veterinary medicines/products
- Medical/pharmacy products
- Wellness products
- Any other products currently available in the catalogue

Every product must have a clear, intentional way to proceed.

For products that can be purchased normally:

Add to Cart / Order
→ Cart
→ Checkout
→ Order

For products that require a phone call or manual consultation:

Show a clear action such as:

Call to Order
or
Contact Pharmacy

Do NOT allow a product to appear orderable if its actual flow cannot work.

Do not create buttons that look clickable but do nothing.

==================================================
2. FIX THE "ORDER" BUTTON
==================================================

Inspect the current "Order" button behavior on product cards.

Determine what it is supposed to do for each product type.

It must either:

A. Add the product correctly to the cart and continue the normal checkout flow

OR

B. Start the intended call/contact flow for products that require it.

The behavior must be deliberate and consistent.

Do not randomly redirect users to Home.

==================================================
3. FIX THE CART FLOW
==================================================

The cart shown in the current application must work smoothly.

Test:

- Open cart
- Add product
- Increase quantity
- Decrease quantity
- Remove product
- Update totals
- Close cart
- Reopen cart
- Proceed to checkout

Make sure cart state is preserved correctly.

Do not lose the selected product when navigating.

==================================================
4. FIX "PROCEED TO CHECKOUT"
==================================================

The current Proceed to Checkout flow must reliably work.

Clicking:

Proceed to Checkout

must actually open the checkout page.

Preserve:

- Products
- Quantities
- Prices
- Subtotal
- Delivery information
- Total

Do not redirect to Home.

Do not leave the user waiting indefinitely.

==================================================
5. FIX THE LAG / SLOWNESS
==================================================

This is very important.

Simple actions such as:

- Clicking Order
- Adding to cart
- Opening cart
- Changing quantity
- Proceeding to checkout

should feel immediate.

Inspect for:

- Unnecessary API calls
- Duplicate API calls
- Repeated React renders
- Unnecessary database requests
- Blocking requests
- Excessive loading states
- Long-running effects
- Duplicate event handlers
- Requests that are repeated when the same data is already available

Optimize the existing implementation.

Do NOT simply hide the delay with a spinner.

The actual operation should become faster.

==================================================
6. PRODUCT TYPE LOGIC
==================================================

Make the product model support a clear ordering mode.

For example, products can have a state/type concept such as:

PURCHASABLE
PRESCRIPTION_REQUIRED
CALL_TO_ORDER
UNAVAILABLE

Use the existing database/schema when possible.

Do not force every product into the same ordering flow.

Examples:

Normal product:
→ Add to Cart
→ Checkout

Prescription product:
→ Add to Cart / Request
→ Prescription submission/review
→ Order confirmation

Call-to-order product:
→ Call Pharmacy
or
→ Contact Pharmacy

Unavailable:
→ Clearly show unavailable state
→ Do not allow checkout

Make the UI communicate which flow applies.

==================================================
7. PRESCRIPTION PRODUCTS
==================================================

For prescription medicines, do not silently treat them as ordinary products.

The customer should have a clear prescription/order flow.

For example:

Select medicine
→ Submit required prescription/details
→ Order/payment workflow
→ Admin review
→ Confirm order

Keep the actual review step that we already discussed.

==================================================
8. VETERINARY PRODUCTS
==================================================

Veterinary products must also have a working ordering path.

Do not assume that a veterinary product is broken just because it is not a normal human-medicine product.

Check the actual product type and give it the appropriate action.

If it is purchasable:
→ normal checkout

If it requires contact:
→ call/contact flow

Make sure every current veterinary product has a functional action.

==================================================
9. MEDICINE REQUEST FLOW
==================================================

The medicine-request feature must also work correctly.

Do not send the user back to Home after submitting a request.

Instead:

Submit request
→ save request
→ show confirmation
→ show request/order status

The user should remain inside the relevant workflow.

==================================================
10. CHECKOUT TOTALS
==================================================

Make sure all calculations are correct.

For example:

Product subtotal     ₹100
Delivery             ₹X
GST/other applicable ₹X
------------------------
Final total          ₹X

Use proper decimal handling.

Values such as:

143.54
267.15
367.15

must remain accurate.

Do not round values incorrectly.

==================================================
11. CUSTOMER ORDER STATUS
==================================================

After a customer successfully submits an order, do not send them randomly to the homepage.

Show an order confirmation/tracking page.

The customer should see:

- Order number
- Products
- Quantity
- Subtotal
- Delivery
- Total
- Payment status
- Order status
- Prescription status where applicable
- Delivery status when available

==================================================
12. ADMIN SIDE
==================================================

Every order created from any product type must appear correctly in the Admin Panel.

Admin should be able to identify:

- Product type
- Customer
- Order number
- Quantity
- Price
- Total
- Payment status
- Prescription status
- Order status
- Delivery status

Admin actions must actually update the backend/database.

==================================================
13. BROKEN IMAGE / PRODUCT UI
==================================================

While testing product cards, also check that product images load correctly.

If a product image is missing or broken:

- Show a clean fallback image
- Do not show broken-image UI
- Do not allow the broken image to disturb card layout

Keep cards visually consistent.

==================================================
14. RESPONSIVE PERFORMANCE
==================================================

Check the ordering flow on:

- Desktop
- Mobile
- Smaller screens

The cart and checkout must not become slow or awkward on mobile.

==================================================
15. FULL END-TO-END TEST
==================================================

Test at least one example of EACH product flow:

GENERAL PRODUCT:
Product
→ Order/Add to Cart
→ Cart
→ Checkout
→ Order

PRESCRIPTION PRODUCT:
Product
→ Prescription/request flow
→ Checkout/order
→ Admin review

VETERINARY PRODUCT:
Product
→ Appropriate order/contact flow
→ Successful submission

CALL-TO-ORDER PRODUCT:
Product
→ Call/contact action
→ No broken checkout

Also test:

- Add to cart
- Quantity changes
- Remove from cart
- Checkout
- Order submission
- Medicine request
- Admin order visibility

==================================================
FINAL REQUIREMENT
==================================================

The application must feel fast and predictable.

Every product should answer one simple question for the customer:

"How do I get this?"

There should never be a product with an Order button that leads nowhere.

There should never be a checkout button that does nothing.

There should never be a successful order that unexpectedly redirects to Home.

There should never be unnecessary waiting for a simple cart/order action.

Fix the ROOT CAUSE of the lag and broken flows.

Do not rewrite unrelated parts of the application.
Do not remove existing working functionality.
Do not create fake functionality just to make buttons appear active.

Make the complete product → cart → checkout → order workflow reliable for ALL supported product types.