Proceed with ONLY the development database provisioning task first.

The verified Preview error is:

POST /api/sync/session
→ HTTP 500
→ relation "sync_sessions" does not exist

The development PostgreSQL database is missing the required schema.

IMPORTANT:

Do NOT manually invent table definitions.

Do NOT create simplified replacement tables.

Do NOT modify the production Render database.

Do NOT delete any existing data.

Do NOT change the medicine importer logic yet.

Do NOT change Vercel configuration.

Do NOT modify:
artifacts/ayush-medico-app
artifacts/ayush-medico-storefront

First inspect the repository's existing database schema, migrations, Drizzle/Prisma/schema files, or database initialization mechanism used by @workspace/db and api-server.

Identify the authoritative schema for:

- sync_sessions
- settings
- medicines
- stock
- company/catalog tables
- categories
- products
- any other tables required by the sync/import/search flow

Then provision the Replit DEVELOPMENT PostgreSQL database using the project's existing authoritative schema/migration mechanism.

Do not create fake/sample medicine data.

After provisioning:

1. Verify the required relations exist.
2. Verify the API can connect to the development database.
3. Run:
   POST /api/sync/session
   using the normal authenticated Preview flow.
4. Confirm that it no longer fails with:
   relation "sync_sessions" does not exist
5. Test the complete Preview inventory-sync flow with the actual SDF files.
6. Verify that the importer writes records to the development PostgreSQL database.
7. Verify that the medicine search reads those imported records.
8. Verify that a successful sync makes the imported medicines visible through the actual medicine-search path.

If any migration/schema problem is found, fix the schema/migration source rather than creating an ad-hoc database structure.

Run:

- database/schema verification
- API TypeScript check
- frontend TypeScript check
- API production build
- frontend production build
- git diff --check

Do not deploy yet.

Do not touch the production database.

At the end report:

A. Which existing schema/migration was used
B. Which development tables were provisioned
C. Whether POST /api/sync/session now works
D. Whether Preview inventory sync completes
E. Whether imported medicines become visible in Preview search
F. Any code files changed
G. Any database/schema files changed
H. Verification results
I. Whether production still requires a separate investigation