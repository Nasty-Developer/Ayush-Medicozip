STOP FEATURE DEVELOPMENT.

This is the AYUSH MEDICO project:
https://github.com/Nasty-Developer/Ayush-Medicozip

Fix ONLY the Render production frontend root 404.

Current Render logs show:

Error: ENOENT: no such file or directory, stat
'/opt/render/project/src/artifacts/api-server/artifacts/ayush-medico/dist/public/index.html'

The actual production frontend build is located at:

/opt/render/project/src/artifacts/ayush-medico/dist/public/index.html

The Express API server is running successfully on PORT 10000 and the database is configured.

The problem is that the frontend static-file path is being resolved relative to:

artifacts/api-server/

instead of the workspace/project root.

TASK:

1. Inspect the existing Express production static-file serving code.
2. Fix the frontend DIST path so it resolves correctly from the Render workspace root.
3. It must resolve to:

artifacts/ayush-medico/dist/public

4. Do NOT hardcode an absolute Render filesystem path.
5. Use a robust path based on process.cwd() or an equivalent workspace-root-safe resolution.
6. Preserve the existing /api/* routes.
7. Preserve SPA fallback routing.
8. Do not change application features.
9. Do not change the database.
10. Do not change Firebase.
11. Do not change the catalogue.
12. Do not rebuild or re-import the project.

VERIFY LOCALLY:

- GET / → HTTP 200
- GET /medicines → HTTP 200
- GET /categories → HTTP 200
- GET /cart → HTTP 200
- GET /checkout → HTTP 200
- GET /api/healthz → HTTP 200 with {"status":"ok"}

Also verify that the resolved frontend path actually exists before starting the server.

Then:

1. git status
2. git diff
3. Commit ONLY this deployment fix.
4. Push to the existing Ayush-Medicozip repository main branch.
5. Do NOT force-push.
6. Do NOT reset or discard existing work.

Commit message:

fix: resolve Render frontend static path

FINAL REPORT:

- Exact file changed
- Exact path resolution used
- Local / response
- Local /api/healthz response
- Commit hash
- Push result