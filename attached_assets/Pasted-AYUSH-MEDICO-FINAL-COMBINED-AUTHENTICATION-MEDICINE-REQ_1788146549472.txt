AYUSH MEDICO — FINAL COMBINED AUTHENTICATION + MEDICINE REQUEST/ORDER FIX

I need you to fix TWO major areas of the Ayush Medico application:

1. CUSTOMER AUTHENTICATION
2. MEDICINE REQUEST / ORDER BUTTONS AND FLOW

IMPORTANT:
The Replit preview currently works much better for customer authentication.

Therefore, DO NOT blindly rewrite the Firebase authentication implementation.

The main problem is that the same application works in Replit but behaves incorrectly on the Render production deployment.

Treat this as BOTH:
- a production environment/configuration problem for authentication
- a genuine functionality problem for medicine-request/order buttons

==================================================
PART 1 — CUSTOMER AUTHENTICATION
==================================================

CURRENT SITUATION:

Replit preview:
- Customer Sign Up works
- Customer Email/Password Sign In works
- Customer Sign Out works
- Google Sign In is being tested

Render production:
https://ayush-medicozip-4.onrender.com

Customer authentication is not behaving correctly there.

Google Sign-In currently opens the Firebase OAuth handler but the flow does not reliably complete.

Email/password authentication can also load without properly completing.

IMPORTANT:
Do NOT rewrite or replace working customer authentication code unless investigation proves that the implementation itself is the problem.

FIRST compare the working Replit environment with the failing Render production environment.

Check:

1. Firebase project configuration
2. Firebase environment variables
3. VITE_FIREBASE_API_KEY
4. VITE_FIREBASE_AUTH_DOMAIN
5. VITE_FIREBASE_PROJECT_ID
6. VITE_FIREBASE_STORAGE_BUCKET
7. VITE_FIREBASE_MESSAGING_SENDER_ID
8. VITE_FIREBASE_APP_ID
9. VITE_FIREBASE_MEASUREMENT_ID
10. Production hostname/origin
11. Firebase Authorized Domains
12. Google provider configuration
13. OAuth popup/redirect behavior
14. Vite production build behavior
15. Environment variables available at BUILD TIME
16. Service worker / PWA behavior
17. Any production-only authentication code paths

The correct Firebase project is:

projectId:
ayush-medico

Do NOT use the old Firebase project.

The Render production hostname is:

ayush-medicozip-4.onrender.com

Make sure the production domain is supported by Firebase Authentication.

==================================================
AUTHENTICATION ERROR HANDLING
==================================================

Do not silently fail.

Do not leave the customer stuck on a loading screen.

Do not convert every Firebase error into a generic message.

Handle actual Firebase errors correctly, including where relevant:

auth/invalid-credential
auth/popup-closed-by-user
auth/popup-blocked
auth/cancelled-popup-request
auth/unauthorized-domain
auth/network-request-failed
auth/account-exists-with-different-credential

For Google Sign-In:

If the popup actually closes by the user, report that.

If the popup closes because the OAuth flow failed, show the actual underlying error during debugging instead of incorrectly telling the user they cancelled it.

Make sure the Google flow is:

Click Google Sign In
→ Google/Firebase popup or redirect
→ authentication succeeds
→ Firebase user returned
→ auth state updates
→ sign-in modal closes
→ customer is signed in

Make sure email/password is:

Email + password
→ Firebase signInWithEmailAndPassword
→ success
→ auth state updates
→ customer is signed in

Make sure sign out is:

Sign Out
→ Firebase signOut()
→ local auth state clears
→ customer is signed out

Do not leave the loading state active forever.

Use proper try/catch/finally handling.

==================================================
AUTH STATE
==================================================

Use a correct Firebase auth-state listener.

Make sure:

onAuthStateChanged(auth, user)

updates the UI correctly.

When the user exists:
- show signed-in state

When the user is null:
- show signed-out state

Do not wait for unrelated data such as products, medicines, admin data, or other API requests before determining the authentication state.

Avoid duplicate Firebase listeners.

Avoid unnecessary repeated authentication requests.

==================================================
IMPORTANT AUTH TEST
==================================================

Because Replit already works, compare REPLIT vs RENDER.

Do not just say "Firebase auth is broken."

Find the exact difference.

If the code is the same but Render still fails, focus on:

- build-time environment variables
- production origin
- Firebase Authorized Domains
- Google OAuth configuration
- service-worker interference
- stale production build
- production-only configuration

The goal is:

WORKING REPLIT AUTH
        ↓
identify difference
        ↓
FIX RENDER
        ↓
RENDER AUTH WORKS

Do not break the working Replit behavior.

==================================================
PART 2 — MEDICINE REQUEST / ORDER BUTTONS
==================================================

The second major problem is that many medicine/order/request buttons are not actually functioning correctly.

Find EVERY button and action related to:

- Request Medicine
- Request a Medicine
- Medicine Request
- Submit Request
- Order
- Add to Cart
- Call to Order
- Proceed to Checkout
- Submit Order
- Upload Prescription
- Prescription Request
- View Request
- Track Request
- Confirm Order
- Continue

Do a complete audit.

Every visible button must have a real, working action.

Do not leave placeholder buttons.

Do not make something look clickable if it does nothing.

==================================================
PRODUCT ORDERING
==================================================

Support all current product types, including:

- General medicines
- Prescription medicines
- Veterinary products
- Medical products
- Wellness/general pharmacy products
- Any other catalogue items

Each product needs the correct ordering behavior.

NORMAL PURCHASABLE PRODUCT:

Product
→ Add to Cart / Order
→ Cart
→ Checkout
→ Order

PRESCRIPTION PRODUCT:

Product
→ Prescription/request flow
→ Required details
→ Payment/order flow
→ Admin review
→ Confirmation

CALL-TO-ORDER PRODUCT:

Product
→ Call Pharmacy / Contact Pharmacy

UNAVAILABLE PRODUCT:

Product
→ Clearly unavailable
→ Do not allow broken checkout

Do not force every product into the same flow.

==================================================
FIX REQUEST MEDICINE
==================================================

When the customer clicks Request Medicine:

Request Medicine
→ request form/page/modal opens
→ customer enters medicine details
→ submits request
→ backend receives request
→ request is saved
→ customer receives confirmation
→ customer sees request/order status

Do NOT send the customer unexpectedly back to Home.

After submission, show a proper confirmation screen/page.

For example:

"Medicine request submitted successfully."

Show a request/reference number where available.

==================================================
PRESCRIPTION REQUESTS
==================================================

For prescription-required medicines:

Customer submits request
→ prescription information saved
→ request goes to admin
→ admin reviews prescription
→ admin approves/rejects
→ customer sees updated status

Do not automatically approve prescription requests just because the customer submitted them.

==================================================
CART
==================================================

Fix the complete cart flow.

Test:

Add product
→ cart opens
→ quantity +
→ quantity -
→ remove
→ totals update
→ cart persists
→ proceed to checkout

Do not lose the cart during navigation.

Do not redirect the customer to Home unexpectedly.

==================================================
CHECKOUT
==================================================

Fix "Proceed to Checkout".

It must actually open the checkout page.

Preserve:

- Product
- Quantity
- Unit price
- Subtotal
- Delivery charge
- GST/other applicable charges
- Final total

Decimal values must work correctly.

Examples:

143.54
267.15
367.15

must remain accurate.

Do not convert decimal values into integers.

==================================================
ORDER FLOW
==================================================

After the customer successfully submits an order:

Do NOT redirect randomly to Home.

Show:

Order number
Products
Quantities
Subtotal
Delivery charge
Total
Payment status
Order status
Prescription status where applicable
Delivery status where available

The customer should remain inside the order flow.

==================================================
CURRENT UPI PAYMENT
==================================================

DO NOT remove the current UPI payment system yet.

We are still waiting for the future Cashfree integration.

For now:

Keep the existing UPI/QR payment flow working.

Do NOT add Cashfree yet.

Do NOT add Razorpay.

Do NOT remove the existing UPI payment setup.

Do NOT require the customer to type a transaction ID.

Use the existing simple customer confirmation button such as:

"I Have Completed the Payment"

After the customer clicks it:

paymentStatus =
VERIFICATION_PENDING

Do NOT mark payment as automatically verified.

Show the customer a message such as:

"Confirming your payment and placing your order."

Then keep the customer on the order confirmation/tracking page.

==================================================
ADMIN PAYMENT VERIFICATION
==================================================

Admin must be able to see the order.

Admin verifies the payment manually for now.

Customer confirmation ≠ payment verification.

Only the admin can currently move:

VERIFICATION_PENDING
→
PAID

After admin verifies the payment:

Order can move to:

CONFIRMED
or
PREPARING

The customer must see the updated status.

==================================================
ADMIN ORDER ACTIONS
==================================================

Make sure the admin panel correctly handles:

Payment verification
Order confirmation
Preparing order
Prescription verification
Prescription approval/rejection
Order status updates

Buttons must update the REAL backend/database state.

Do not merely change frontend text.

==================================================
DELIVERY
==================================================

Do NOT automatically integrate or book Borzo yet.

We are still waiting for the Borzo API credentials.

Prepare the order structure so delivery can later be connected.

For now:

PAID
→ CONFIRMED
→ PREPARING

Later, Borzo can be connected.

==================================================
PERFORMANCE
==================================================

The website currently feels laggy during simple actions.

Fix actual causes rather than hiding the delay with spinners.

Inspect:

- duplicate API calls
- duplicate React renders
- unnecessary useEffect calls
- duplicate event listeners
- repeated database requests
- unnecessary product reloads
- blocking requests
- slow cart updates
- slow request submission

Simple actions must feel immediate:

Add to Cart
Open Cart
Change Quantity
Remove
Request Medicine
Submit Request
Proceed to Checkout
Sign In
Sign Out

Prevent duplicate submissions while requests are processing.

Do not create infinite loading states.

==================================================
ERROR HANDLING
==================================================

No dead buttons.

No silent failures.

No unexpected redirects.

No infinite spinners.

Every failed API/action should show a clear user-friendly message.

Log the real technical error in development/debugging without exposing secrets.

==================================================
FINAL FULL TEST
==================================================

After implementation, test the COMPLETE application.

CUSTOMER AUTH:
- Sign Up
- Email/Password Sign In
- Google Sign In
- Sign Out
- Sign In again
- Refresh while signed in
- Refresh while signed out

PRODUCTS:
- General medicine
- Prescription medicine
- Veterinary product
- General pharmacy product

ORDERING:
- Add to Cart
- Quantity changes
- Remove
- Checkout
- Submit order

REQUEST:
- Request Medicine
- Submit request
- Request confirmation
- Prescription request

PAYMENT:
- Existing UPI flow
- "I Have Completed the Payment"
- Payment Verification Pending
- Admin verification

ADMIN:
- Order appears
- Payment verification
- Prescription verification
- Confirm order
- Preparing status

FINAL CUSTOMER STATUS:
Customer sees all updated order/request states correctly.

==================================================
VERY IMPORTANT FINAL RULES
==================================================

DO NOT:

- Rewrite working authentication without evidence
- Remove the current UPI system
- Add Cashfree yet
- Add Razorpay
- Remove working features
- Create fake payment verification
- Create fake delivery confirmation
- Redirect customers to Home after successful requests/orders
- Leave dead buttons
- Hard-code secret API credentials
- Put backend secrets in frontend code

The goal is a reliable production application where:

CUSTOMER AUTH WORKS
+
GOOGLE AUTH WORKS
+
SIGN OUT WORKS
+
REQUEST MEDICINE WORKS
+
ALL ORDER BUTTONS WORK
+
CART WORKS
+
CHECKOUT WORKS
+
CURRENT UPI ORDER FLOW WORKS
+
ADMIN VERIFICATION WORKS
+
CUSTOMER SEES ORDER STATUS
+
APPLICATION FEELS FAST

Most importantly:

Replit already proves that customer authentication can work.

Do not destroy that working behavior.

Find what is different in Render and fix the production issue.