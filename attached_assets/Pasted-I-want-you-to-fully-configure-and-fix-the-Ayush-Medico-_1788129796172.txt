I want you to fully configure and fix the Ayush Medico application.

IMPORTANT:
Do not ask me to manually copy Firebase values again.
Use the exact Firebase configuration below and add the corresponding values to the Replit Secrets / environment variables yourself.

==================================================
REAL AYUSH MEDICO FIREBASE CONFIGURATION
==================================================

VITE_FIREBASE_API_KEY=AIzaSyBKHUUgd5YYHAfJxGEBKRWSDc7kd3mhMLM
VITE_FIREBASE_AUTH_DOMAIN=ayush-medico.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=ayush-medico
VITE_FIREBASE_STORAGE_BUCKET=ayush-medico.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=393423004675
VITE_FIREBASE_APP_ID=1:393423004675:web:45e2d434b8c0633cd89a36
VITE_FIREBASE_MEASUREMENT_ID=G-V81ZNN4YDX

These values belong to the REAL Firebase project:

ayush-medico

Do not use the older Firebase project configuration.
Do not invent or modify any of these values.

Add them to Replit Secrets correctly and make sure the application reads them from environment variables.

==================================================
1. FIX FIREBASE AUTHENTICATION
==================================================

The current application has authentication problems.

Fix the complete authentication system.

Sign In must:
- Work reliably
- Be fast
- Show an immediate loading state
- Correctly authenticate with Firebase
- Update the UI immediately after authentication
- Show useful errors when authentication fails
- Preserve the session after refresh

Sign Out must:
- Work reliably
- Be fast
- Clear the authenticated user correctly
- Clear stale admin/user state
- Return immediately to the signed-out UI

Google Sign In must also work correctly.

Do not make authentication wait for unrelated application data.

==================================================
2. FIX AUTHORIZED DOMAIN
==================================================

The production website is hosted on:

ayush-medicozip-4.onrender.com

Make sure the Firebase Authentication setup works correctly for this production domain.

The application must not produce:

auth/unauthorized-domain

Do not bypass or weaken authentication security.

If something must be configured in Firebase itself and cannot be changed from the codebase, clearly identify exactly what needs to be configured.

==================================================
3. FIX ADMIN ACCESS
==================================================

The intended admin account must be able to access the admin panel.

Make sure the admin authorization logic correctly detects the authenticated admin email.

Use the existing admin configuration/environment variable.

Make sure:
- Admin sign-in works
- Admin authorization works
- Admin access survives refresh
- Admin sign-out works
- Signing into another account does not leave stale admin access
- The admin panel does not incorrectly show "Access denied"

==================================================
4. FIX THE ENTIRE ADMIN PANEL
==================================================

Make every existing admin feature actually work.

Check all:
- Buttons
- Forms
- Add medicine
- Edit medicine
- Save medicine
- Delete medicine
- Product management
- Stock management
- Category management
- Price editing
- Order/request management
- Existing admin navigation

IMPORTANT:

Decimal values MUST work correctly.

For example:

143.54

must be:
- Accepted
- Displayed correctly
- Editable
- Saved correctly
- Loaded correctly after refresh

Do not convert decimal numbers into integers.
Do not reject valid decimal values.
Do not silently alter the value.

==================================================
5. CHECK ALL USER FEATURES
==================================================

Verify the complete user application:

- Sign up
- Sign in
- Google sign in
- Sign out
- Session persistence
- Navigation
- Categories
- Medicine/product browsing
- Product details
- Add to cart
- Increase/decrease quantity
- Remove from cart
- Cart totals
- Medicine requests
- Forms
- API communication
- Loading states
- Error states

Fix anything that is actually broken.

==================================================
6. AUTHENTICATION PERFORMANCE
==================================================

The sign-in and sign-out experience currently feels too slow.

Improve the implementation so that:

- Firebase initialization is handled correctly
- Authentication state is managed efficiently
- Duplicate auth listeners are avoided
- Unnecessary API calls are avoided
- The UI does not wait for unrelated data before showing authentication state
- Loading states are clear
- There is no fake infinite loading
- Sign-in and sign-out feel immediate and reliable

==================================================
7. DO NOT BREAK EXISTING FUNCTIONALITY
==================================================

The existing application already contains many working features.

Do NOT rewrite the entire project unnecessarily.

Do NOT remove working functionality.
Do NOT replace working APIs without a real reason.
Do NOT change the database structure unless required.
Do NOT remove existing admin features.

Fix problems while preserving what already works.

==================================================
8. FINAL TEST
==================================================

After making the fixes, actually test everything.

Test:

1. Sign up
2. Sign in with email/password
3. Google sign in
4. Sign out
5. Refresh while signed in
6. Refresh while signed out
7. Admin login
8. Admin panel access
9. Add medicine
10. Edit medicine
11. Enter a decimal price such as 143.54
12. Save it
13. Refresh and verify 143.54 remains correct
14. Delete/edit other medicine fields
15. Add a product to cart
16. Change quantity
17. Remove product
18. Submit a medicine request
19. Check backend/API communication
20. Check browser console
21. Check for Firebase authentication errors
22. Check for runtime errors

Do not consider the task complete merely because the homepage loads.

The final application must have:

WORKING FIREBASE
+
WORKING SIGN IN
+
WORKING GOOGLE SIGN IN
+
WORKING SIGN OUT
+
WORKING ADMIN AUTHORIZATION
+
WORKING ADMIN PANEL
+
WORKING MEDICINE EDITING
+
WORKING DECIMAL VALUES
+
WORKING CART
+
WORKING MEDICINE REQUESTS
+
FAST AUTHENTICATION
+
NO AUTH/UNAUTHORIZED-DOMAIN ERROR

Use the Firebase values provided above and configure the Replit Secrets/environment variables yourself.