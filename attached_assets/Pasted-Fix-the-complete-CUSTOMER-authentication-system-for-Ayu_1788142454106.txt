Fix the complete CUSTOMER authentication system for Ayush Medico.

IMPORTANT:
This is for NORMAL CUSTOMERS/USERS.
Do not make this an admin-only fix.
Do not change or remove the admin authorization system.

The production site is currently returning:

Firebase: Error (auth/invalid-credential)

when a customer tries to sign in.

Fix the complete authentication flow from beginning to end.

==================================================
1. FIREBASE CONFIGURATION
==================================================

Verify that the application is using the correct Firebase project:

projectId:
ayush-medico

Use the Firebase configuration already provided for this project.

Make sure the application reads the Firebase configuration from the correct environment variables/secrets.

Do NOT switch to the old Firebase project.
Do NOT invent new Firebase credentials.
Do NOT hard-code credentials unnecessarily.

==================================================
2. CUSTOMER SIGN UP
==================================================

Make sure a normal customer can:

Sign Up
→ Create Firebase account
→ Account is created successfully
→ User is authenticated
→ Application recognizes the user
→ User is taken into the normal signed-in state

Make sure sign-up errors are handled properly.

Do not accidentally create duplicate accounts.

==================================================
3. CUSTOMER EMAIL/PASSWORD SIGN IN
==================================================

Fix the current:

auth/invalid-credential

problem.

Verify:

- Firebase Email/Password authentication is enabled
- Correct Firebase project is being used
- signInWithEmailAndPassword is implemented correctly
- Email is handled correctly
- Password is handled correctly
- No incorrect transformation of credentials occurs
- No unnecessary API request blocks authentication
- Auth errors are handled correctly

A valid customer should be able to:

Enter email
+
Enter password
→
Sign In
→
Firebase authenticates
→
Authenticated user state updates
→
Customer is signed in

Do NOT automatically create an account when login fails.

Do NOT bypass password verification.

==================================================
4. CUSTOMER GOOGLE SIGN IN
==================================================

Make sure:

Continue with Google

works correctly.

The Google authentication flow should:

Open Google authentication
→
Authenticate
→
Return to the website
→
Update Firebase auth state
→
Show the customer as signed in

Do not break the Google sign-in flow while fixing email/password authentication.

==================================================
5. CUSTOMER SIGN OUT
==================================================

Fix sign-out completely.

When the customer clicks Sign Out:

Sign Out
→
Firebase signOut()
→
Authenticated user state clears
→
User becomes signed out
→
Signed-out UI appears immediately

Make sure no stale user information remains.

After signing out, the customer should be able to sign in again normally.

==================================================
6. AUTH STATE / SESSION
==================================================

Handle Firebase authentication state correctly.

Use a proper Firebase auth state listener.

Make sure:

- Refreshing the page while signed in keeps the customer signed in
- Refreshing while signed out keeps the customer signed out
- The UI does not briefly show incorrect authentication state
- The application does not repeatedly initialize authentication
- Duplicate auth listeners are avoided
- Auth initialization does not unnecessarily block the entire website

==================================================
7. AUTH PERFORMANCE
==================================================

Sign in and sign out currently feel slower than they should.

Improve the authentication UX.

When authentication is happening:

- Show an immediate loading state
- Disable duplicate clicks
- Do not make the user think the website is frozen
- Update authentication state as soon as Firebase completes
- Do not wait for unrelated medicine/product/API requests before showing signed-in state

Authentication should be independent from unrelated application loading.

==================================================
8. AUTHORIZED DOMAIN
==================================================

The production website is hosted at:

ayush-medicozip-4.onrender.com

Make sure Firebase Authentication is correctly configured for the production domain.

Avoid:

auth/unauthorized-domain

Do not weaken authentication security just to remove the error.

==================================================
9. CUSTOMER VS ADMIN
==================================================

This is the NORMAL CUSTOMER authentication system.

Do NOT require customers to be admins.

Do NOT route every customer into the admin system.

The flow should be:

Normal customer
→
Sign Up / Sign In
→
Customer account
→
Website

The admin system must remain separate.

The existing admin account and admin authorization logic should continue working.

==================================================
10. ERROR HANDLING
==================================================

Show useful errors for:

- Invalid email/password
- User does not exist
- Wrong password
- Unauthorized domain
- Google authentication failure
- Network/API failure
- Firebase configuration problems

Do not hide every Firebase error behind:

"Something went wrong"

Where possible, show a clear user-friendly message.

Do not expose sensitive internal credentials or backend secrets.

==================================================
11. TEST THE COMPLETE CUSTOMER FLOW
==================================================

After fixing the code, test all of these:

1. New customer Sign Up
2. Customer Email/Password Sign In
3. Customer Google Sign In
4. Customer Sign Out
5. Sign In again after Sign Out
6. Refresh while signed in
7. Refresh while signed out
8. Navigate between pages while signed in
9. Add product to cart while signed in
10. Checkout while signed in
11. Verify that customer authentication remains intact
12. Check browser console for Firebase/runtime errors

Test both successful and failed authentication attempts.

==================================================
12. IMPORTANT
==================================================

Do not rewrite the entire application.

Do not change unrelated parts of the website.

Do not remove working features.

Do not modify the admin system unnecessarily.

Fix the ROOT CAUSE of the authentication problems.

The final customer authentication system must provide:

WORKING SIGN UP
+
WORKING EMAIL/PASSWORD SIGN IN
+
WORKING GOOGLE SIGN IN
+
WORKING SIGN OUT
+
WORKING SESSION PERSISTENCE
+
FAST AUTH UI
+
CORRECT FIREBASE PROJECT
+
NO UNAUTHORIZED-DOMAIN ERROR
+
NO INVALID-CREDENTIAL PROBLEM FOR VALID ACCOUNTS

Verify the complete customer flow after making the changes.