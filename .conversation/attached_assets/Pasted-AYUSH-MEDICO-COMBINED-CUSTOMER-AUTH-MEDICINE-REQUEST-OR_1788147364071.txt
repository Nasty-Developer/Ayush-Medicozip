AYUSH MEDICO — COMBINED CUSTOMER AUTH + MEDICINE REQUEST + ORDERING FIX

I need you to fix TWO major areas of the Ayush Medico application:

1. CUSTOMER AUTHENTICATION
2. MEDICINE REQUEST / PRODUCT ORDERING / CHECKOUT FLOW

IMPORTANT:
The Replit preview currently works much better for customer authentication than the Render production deployment.

DO NOT blindly rewrite the Firebase authentication system.

First understand the existing implementation and identify why the production deployment behaves differently.

==================================================
PART 1 — CUSTOMER AUTHENTICATION
==================================================

CURRENT SITUATION:

Replit preview:
- Customer Sign Up works
- Customer Email/Password Sign In works
- Customer Sign Out works

Render production:
https://ayush-medicozip-4.onrender.com

Authentication is not behaving correctly there.

Google Sign-In can open the Firebase authentication popup but may fail to complete.

Email/password authentication can also load without completing properly.

IMPORTANT:
Do NOT replace Firebase Authentication.
Do NOT change the Firebase project.
Do NOT rewrite working authentication unless investigation proves it is actually necessary.

The correct Firebase project is:

projectId:
ayush-medico

==================================================
COMPARE REPLIT VS RENDER
==================================================

Because authentication works in Replit but not consistently in Render, compare the environments.

Check:

- Firebase configuration
- VITE_FIREBASE_API_KEY
- VITE_FIREBASE_AUTH_DOMAIN
- VITE_FIREBASE_PROJECT_ID
- VITE_FIREBASE_STORAGE_BUCKET
- VITE_FIREBASE_MESSAGING_SENDER_ID
- VITE_FIREBASE_APP_ID
- VITE_FIREBASE_MEASUREMENT_ID
- Firebase Authorized Domains
- Production hostname
- OAuth popup/redirect configuration
- Production Vite build
- Build-time environment variables
- Service worker / PWA behavior
- Production-only code paths

Production hostname:

ayush-medicozip-4.onrender.com

Find the exact difference between the working Replit environment and the failing Render environment.

Do NOT just replace credentials randomly.

==================================================
EMAIL/PASSWORD SIGN IN
==================================================

Make sure:

Email
+
Password
→ Firebase authentication
→ authenticated user state updates
→ customer becomes signed in

Use proper try/catch/finally handling.

Make sure the loading state can NEVER remain stuck forever.

Do not silently swallow Firebase errors.

Do not automatically create an account when sign-in fails.

Do not bypass password verification.

==================================================
GOOGLE SIGN IN
==================================================

Make Google Sign-In work correctly on production.

Current behavior can show:

Firebase:
auth/popup-closed-by-user

or the popup can open and fail to complete.

Do not automatically assume the customer manually cancelled it.

Determine the real underlying cause.

Check:

- Firebase Google provider
- Authorized domains
- OAuth configuration
- Popup/redirect behavior
- Production origin
- Browser popup handling
- Firebase configuration
- Production build

After successful Google authentication:

Google/Firebase authentication
→ Firebase user returned
→ auth state updates
→ modal closes
→ customer is signed in

==================================================
SIGN OUT
==================================================

Make sign out reliable:

Sign Out
→ Firebase signOut()
→ authenticated state cleared
→ local user state cleared
→ customer becomes signed out

No stale user/admin state.

Sign in must work again afterward.

==================================================
AUTH STATE
==================================================

Use proper Firebase auth state handling.

Make sure:

onAuthStateChanged(auth, user)

correctly updates customer state.

Do not create duplicate listeners.

Do not wait for unrelated:

- Products
- Medicines
- Cart
- Admin data
- Other APIs

before updating the customer's authentication state.

Authentication should complete independently.

==================================================
ERROR HANDLING
==================================================

Handle actual Firebase errors correctly, including where relevant:

auth/invalid-credential
auth/popup-closed-by-user
auth/popup-blocked
auth/cancelled-popup-request
auth/unauthorized-domain
auth/account-exists-with-different-credential
auth/network-request-failed

Do not convert every technical failure into:

"Google sign-in cancelled."

During debugging, log the real error code without exposing passwords, tokens, or secrets.

==================================================
PART 2 — MEDICINE REQUEST / ORDERING
==================================================

The second major issue is that some medicine request/order buttons do not work correctly.

Audit EVERY related action:

- Request Medicine
- Request a Medicine
- Medicine Request
- Submit Request
- Order
- Add to Cart
- Call to Order
- Proceed to Checkout
- Submit Order
- Upload Prescription
- Prescription Request
- View Request
- Track Request
- Confirm Order
- Continue

Every visible button must have a real working action.

Do not leave placeholder buttons.

Do not make something look clickable when it does nothing.

==================================================
PRODUCT TYPES
==================================================

Check ALL current product types:

- General medicines
- Prescription medicines
- Veterinary products
- Medical products
- Wellness products
- Other pharmacy/general products

Each product must have the correct ordering flow.

NORMAL PRODUCT:

Product
→ Add to Cart / Order
→ Cart
→ Checkout
→ Order

PRESCRIPTION PRODUCT:

Product
→ Prescription / Request flow
→ Required information
→ Order/payment flow
→ Admin review
→ Confirmation

CALL-TO-ORDER PRODUCT:

Product
→ Call Pharmacy / Contact Pharmacy

UNAVAILABLE PRODUCT:

Product
→ Clearly unavailable
→ No broken checkout

Do not force all product types into the same flow.

==================================================
REQUEST MEDICINE
==================================================

Fix the complete Request Medicine flow.

It should be:

Request Medicine
→ Form opens
→ Customer enters required details
→ Submit
→ Backend receives request
→ Request is saved
→ Customer sees confirmation
→ Customer sees request/order status

Do NOT send the customer unexpectedly to Home.

After submitting, show a proper confirmation page or state.

For example:

"Medicine request submitted successfully."

Show a request/reference number when available.

==================================================
PRESCRIPTION FLOW
==================================================

For medicines requiring a prescription:

Customer submits request
→ prescription information saved
→ admin reviews
→ admin approves/rejects
→ customer sees updated status

Do not automatically approve prescription requests.

==================================================
CART
==================================================

Fix the complete cart flow.

Test:

Add product
→ cart opens
→ increase quantity
→ decrease quantity
→ remove product
→ totals update
→ cart persists
→ Proceed to Checkout

Do not lose cart data while navigating.

==================================================
CHECKOUT
==================================================

Fix:

Proceed to Checkout

It must actually open checkout.

Preserve:

- Products
- Quantities
- Unit prices
- Subtotal
- Delivery charge
- GST/other applicable charges
- Final total

Decimal values must work correctly.

Examples:

143.54
267.15
367.15

must remain accurate.

Do not accidentally convert decimal values to integers.

==================================================
CURRENT UPI PAYMENT
==================================================

IMPORTANT:

DO NOT REMOVE THE CURRENT UPI PAYMENT SYSTEM YET.

We are waiting for the future Cashfree integration.

For now:

Keep the existing UPI/QR payment flow.

Do NOT add Cashfree yet.
Do NOT add Razorpay.
Do NOT remove the current UPI system.

Do NOT require the customer to type a transaction ID.

Use one simple button such as:

"I Have Completed the Payment"

When the customer clicks it:

paymentStatus =
VERIFICATION_PENDING

Do NOT automatically mark payment as verified.

Show a confirmation such as:

"Confirming your payment and placing your order."

Then take the customer to the order confirmation/tracking page.

Do NOT send them to Home.

==================================================
ADMIN PAYMENT VERIFICATION
==================================================

The customer clicking:

"I Have Completed the Payment"

only means:

Customer says payment was completed.

It does NOT mean:

Payment is verified.

The admin must verify the payment.

Admin flow:

Order appears
→ Admin checks payment
→ Payment verified
→ paymentStatus = PAID
→ orderStatus = CONFIRMED / PREPARING

Customer should then see the updated status.

==================================================
ADMIN ORDER ACTIONS
==================================================

Ensure the admin panel correctly handles:

- Payment verification
- Prescription verification
- Order confirmation
- Preparing order
- Order status changes

Buttons must update the REAL backend/database.

Do not merely change frontend labels.

==================================================
CUSTOMER ORDER STATUS
==================================================

After submitting an order, do NOT redirect the customer to Home.

Show an order confirmation/tracking page.

Customer should see:

- Order number
- Products
- Quantities
- Subtotal
- Delivery charge
- Total
- Payment status
- Order status
- Prescription status if applicable
- Delivery status when available

Example:

Payment:
Verification Pending

Order:
Payment Verification Pending

After admin verifies:

Payment:
PAID

Order:
CONFIRMED

After preparation:

Order:
PREPARING

==================================================
DELIVERY
==================================================

DO NOT integrate Borzo automatically yet.

We are waiting for Borzo API access.

For now:

PAID
→ CONFIRMED
→ PREPARING

Later we will connect Borzo.

Keep the order structure ready for future delivery integration.

==================================================
PERFORMANCE
==================================================

The website currently feels laggy during simple actions.

Fix the actual causes.

Investigate:

- Duplicate API calls
- Duplicate React renders
- Repeated useEffect execution
- Repeated database requests
- Unnecessary product reloads
- Blocking requests
- Slow cart updates
- Slow request submission
- Authentication waits
- Unnecessary network calls

Do NOT simply hide the delay with a spinner.

Simple actions should feel fast:

- Add to Cart
- Open Cart
- Quantity changes
- Remove
- Request Medicine
- Submit Request
- Proceed to Checkout
- Sign In
- Sign Out

Prevent duplicate submissions.

Never leave the user stuck on an infinite spinner.

==================================================
PRODUCT IMAGES
==================================================

Also check product images.

If an image is missing or fails to load:

- Show a clean fallback
- Do not show broken-image icons
- Do not allow the broken image to distort the card layout

Keep product cards stable.

==================================================
FINAL TESTING
==================================================

After making changes, test the COMPLETE application.

CUSTOMER AUTH:

- Sign Up
- Email/Password Sign In
- Google Sign In
- Sign Out
- Sign In again
- Refresh while signed in
- Refresh while signed out

PRODUCTS:

- General medicine
- Prescription medicine
- Veterinary product
- Medical/pharmacy product
- Other current products

ORDERING:

- Add to Cart
- Quantity +
- Quantity -
- Remove
- Proceed to Checkout
- Submit Order

REQUESTS:

- Request Medicine
- Submit Request
- Request confirmation
- Prescription request

PAYMENT:

- Existing UPI flow
- "I Have Completed the Payment"
- Payment Verification Pending
- Admin payment verification

ADMIN:

- Order appears
- Payment verification works
- Prescription verification works
- Confirm order
- Preparing status

CUSTOMER:

- Customer receives updated order status
- Customer does not get randomly redirected Home
- Customer can view order/reference information

==================================================
CRITICAL RULES
==================================================

DO NOT:

- Rewrite working Firebase authentication without evidence
- Remove the current UPI system
- Add Cashfree yet
- Add Razorpay
- Remove working features
- Create fake payment verification
- Automatically mark payment as PAID from the customer button
- Automatically approve prescriptions
- Create fake delivery confirmation
- Redirect successful requests/orders to Home
- Leave dead buttons
- Hard-code API secrets
- Expose backend secrets in frontend code

The immediate goal is:

CUSTOMER AUTH WORKS
+
RENDER AUTH MATCHES REPLIT
+
GOOGLE AUTH WORKS
+
SIGN OUT WORKS
+
REQUEST MEDICINE WORKS
+
ALL PRODUCT ORDER BUTTONS WORK
+
CART WORKS
+
CHECKOUT WORKS
+
CURRENT UPI PAYMENT FLOW WORKS
+
ADMIN VERIFICATION WORKS
+
CUSTOMER ORDER STATUS WORKS
+
NO UNEXPECTED REDIRECTS
+
NO SIMPLE-ACTION LAG

Most importantly:

Replit already proves that customer authentication can work.

DO NOT DESTROY THE WORKING REPLIT AUTHENTICATION.

Find the production difference and fix Render.

Make the ordering/request system actually functional end-to-end.

Test everything before declaring the task complete.