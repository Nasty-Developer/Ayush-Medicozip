{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "corepack prepare pnpm@10.26.1 --activate && pnpm install --frozen-lockfile && node api/build.mjs && pnpm --filter @workspace/ayush-medico-app run build",
  "outputDirectory": "artifacts/ayush-medico-app/dist/public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/[...path]"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
