We need to finish the Ayush Medico deployment urgently. Treat this as a production fix, not an exploratory rewrite.

## GOAL

The Ayush Medico project is deployed from the repository ROOT on Vercel.

The frontend already builds successfully.

The backend works correctly locally and on Render.

The only thing we need now is for the Vercel deployment to successfully serve the existing medicine API and sync functionality, using the same backend logic already present in the project.

The desired result is:

* GET /api/healthz → HTTP 200
* GET /api/medicines → HTTP 200 with the existing medicines from PostgreSQL
* existing medicine sync/import functionality works on Vercel
* existing frontend continues working
* existing Render deployment must NOT be broken

Do NOT rewrite the application or database.
Do NOT replace the existing medicine data.
Do NOT create fake/mock medicines.
Do NOT change the database schema unless absolutely required.
Do NOT remove existing sync functionality.
Do NOT make unrelated changes.

## CURRENT PROJECT STRUCTURE

This is a pnpm workspace monorepo.

The Vercel project Root Directory is the repository root.

The API server is:

artifacts/api-server

Its package.json contains:

"build": "node ./build.mjs"

"start": "node --enable-source-maps ./dist/index.mjs"

The existing API production build uses esbuild.

artifacts/api-server/build.mjs currently bundles:

src/index.ts

into:

artifacts/api-server/dist/index.mjs

with:

bundle: true

The existing local production backend was tested successfully:

PORT=3000 pnpm --filter @workspace/api-server run build
PORT=3000 node artifacts/api-server/dist/index.mjs

Local results:

GET /api/healthz → 200 {"status":"ok"}
GET /api/medicines → 200

Therefore the backend itself is already functional.

## CURRENT VERCEL API FUNCTION

The repository contains:

api/[...path].ts

Current contents are effectively:

import app from "../artifacts/api-server/src/app";

export const config = {
api: {
bodyParser: false,
},
};

export default app;

The duplicate file:

api/[...path].js

was removed because Vercel reported:

Two or more files have conflicting paths or names.
api/[...path].js conflicts with api/[...path].ts

That duplicate-file problem is already fixed.

## CURRENT vercel.json

The repository root currently contains:

{
"version": 2,
"buildCommand": "pnpm --filter @workspace/ayush-medico run build",
"outputDirectory": "artifacts/ayush-medico/dist/public",
"builds": [
{
"src": "api/[...path].ts",
"use": "@vercel/node"
}
],
"routes": [
{
"src": "/api/(.*)",
"dest": "api/[...path].ts"
},
{
"handle": "filesystem"
},
{
"src": "/(.*)",
"dest": "/index.html"
}
]
}

## ACTUAL VERCEL RUNTIME ERROR

Vercel successfully builds and deploys the project.

The deployment itself completes successfully.

However, requesting:

GET /api/healthz

returns:

500 FUNCTION_INVOCATION_FAILED

The runtime error is:

Error [ERR_MODULE_NOT_FOUND]:

Cannot find module

/var/task/artifacts/api-server/node_modules/@workspace/api-zod/src/index.ts

imported from

/var/task/artifacts/api-server/src/routes/health.js

This proves the problem is Vercel's runtime packaging/module resolution of the raw workspace source tree.

It is NOT a route-name problem.

The real health endpoint is:

/api/healthz

The Express application is mounted under /api.

## IMPORTANT WORKSPACE PACKAGE

lib/api-zod/package.json currently contains:

{
"name": "@workspace/api-zod",
"version": "0.0.0",
"private": true,
"type": "module",
"exports": {
".": "./src/index.ts"
},
"dependencies": {
"zod": "catalog:"
}
}

This raw TypeScript workspace export is what Vercel is attempting to load at runtime.

## EXISTING BACKEND BUILD

artifacts/api-server/build.mjs uses esbuild with:

entryPoints: [path.resolve(artifactDir, "src/index.ts")]

platform: "node"

bundle: true

format: "esm"

outdir: distDir

The external list contains things such as firebase-admin and protobufjs, but @workspace/api-zod and @workspace/db are NOT externalized, meaning the existing backend build is designed to bundle the workspace packages into the production artifact.

This existing build is already known to work locally.

## REQUIRED TASK

Inspect the existing backend architecture, especially:

* artifacts/api-server/src/app.ts
* artifacts/api-server/src/index.ts
* artifacts/api-server/build.mjs
* artifacts/api-server/src/routes/health.*
* medicine routes
* sync routes
* lib/api-zod
* lib/db

Then implement the MINIMUM safe Vercel-specific packaging fix.

The preferred architecture is:

Vercel
→ api/[...path].ts
→ bundled Express application/handler
→ existing workspace dependencies bundled correctly
→ existing PostgreSQL
→ existing medicine routes
→ existing sync routes

Do NOT make the Vercel function import the raw workspace TypeScript dependency graph at runtime.

The existing Render/standalone backend must continue using its existing:

src/index.ts
→ build.mjs
→ dist/index.mjs

Do not break that.

If necessary, create a Vercel-specific esbuild build entry for src/app.ts or an equivalent handler entry that exports the Express app without starting a standalone HTTP server.

The Vercel bundle must preserve:

* Express
* @workspace/api-zod
* @workspace/db
* medicine routes
* sync routes
* PostgreSQL access
* Firebase behavior
* multipart/multer handling
* Razorpay webhook raw-body behavior

Keep bodyParser disabled in the Vercel function.

## VERY IMPORTANT

Do not solve this by changing:

@workspace/api-zod

to some fake compiled path unless the actual build system supports it.

Do not simply change exports from .ts to .js without ensuring those .js files actually exist.

Do not copy random node_modules into the repository.

Do not commit generated node_modules.

Do not remove the database.

Do not replace PostgreSQL with an in-memory database.

Do not hardcode medicine data.

Do not disable authentication.

Do not remove sync routes.

Do not modify the frontend unless required for the API URL.

Do not make broad TypeScript cleanup changes unrelated to getting the production Vercel bundle working.

## VALIDATION REQUIRED

After making the fix, run the actual backend build.

Then verify that the Vercel handler can load without:

ERR_MODULE_NOT_FOUND

Test locally with the closest possible Vercel-compatible execution.

At minimum verify:

GET /api/healthz → 200

GET /api/medicines → 200

Then test the existing sync endpoint if its required request format is known.

Do not invent a fake successful sync response.

Inspect the actual database/API result.

## GIT

Before finishing:

git status

Show exactly which files were changed.

Do not create unrelated changes.

The final changes should be minimal and specifically targeted at Vercel serverless API packaging.

## SUCCESS CRITERIA

The fix is successful only when the Vercel API can load the existing Express application without trying to resolve:

@workspace/api-zod/src/index.ts

at runtime.

The medicine API must be capable of reading the existing PostgreSQL medicine data.

The existing medicine sync functionality must remain intact.

The Render deployment must remain intact.

Do not stop at "build succeeded".
Do not stop at "Vercel deployed".
The actual API runtime must work.

Before making destructive changes, inspect the existing files and preserve the current architecture.
