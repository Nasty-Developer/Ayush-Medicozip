We need to fix the Ayush Medico Inventory Sync production issue.

IMPORTANT: Do NOT make code or database changes until you complete the investigation below and report the findings.

PROBLEM:
Replit Preview Inventory Sync works correctly, but the production Vercel frontend → Render API returns:

"A sync is already in progress for this user."

Example production response:

{
  "error": "A sync is already in progress for this user.",
  "code": "sync_session_already_active",
  "sessionId": "69b93615-860c-4b16-a35f-25b52379d039",
  "retryAt": "2026-08-17T13:11:56.360Z"
}

The production frontend is deployed on Vercel and the API/backend is running on Render.

The latest sync-session fix has already been deployed to production, so investigate the CURRENT production behavior rather than assuming the old code is running.

==================================================
PHASE 1 — INVESTIGATE FIRST
==================================================

Do NOT modify code or production database data during this phase.

Compare Replit Preview and production Render runtime configuration.

Inspect:

1. DATABASE_URL / PostgreSQL database identity
   - Determine whether Replit Preview and Render production use the same PostgreSQL database.
   - Do NOT expose credentials, passwords, connection strings, or secrets.
   - Only report whether they point to the same database or different databases.

2. Firebase project ID
   - Confirm the Firebase project used by the production frontend.
   - Confirm the Firebase project expected by the Render API.
   - Do not expose private keys or tokens.

3. API/server environment
   - Determine which API implementation Replit Preview uses.
   - Determine which API implementation Render production uses.
   - Confirm the production request path for /api/sync/session.

4. sync_sessions
   - Inspect the schema and session-lock logic.
   - Determine how uploading/running sessions are identified.
   - Determine how expires_at is calculated.
   - Determine whether the production session shown above could legitimately still be active.

5. settings / sync:current_job
   - Determine whether the cross-instance job guard is involved in this particular 409 response.
   - Do not modify or delete the production row.

6. Compare the actual production and Replit database/runtime behavior.

At the end of Phase 1, report:

- Same database or different database
- Same Firebase project or different project
- Replit API environment
- Render API environment
- Current lock mechanism responsible for the 409
- Whether session 69b93615-860c-4b16-a35f-25b52379d039 appears active or stale
- Whether the issue is caused by different databases, stale session state, lease design, or another concrete cause

Do NOT guess. If something cannot be inspected, explicitly say so.

==================================================
PHASE 2 — IMPLEMENT THE SAFE FIX
==================================================

After the investigation, implement the fix.

Keep the existing PostgreSQL concurrency protection and partial unique index.

Do NOT remove concurrency protection.

Do NOT manually delete or modify production inventory/session records.

Do NOT change Firebase authentication.

Do NOT change the Vercel frontend architecture.

Do NOT move the Vercel root directory.

Do NOT replace the Render backend with another architecture.

Fix the session lease design safely:

1. Sessions in "uploading" state must use a short inactivity lease of approximately 10–15 minutes.

2. Sessions in "running" state must use a longer lease appropriate for the actual import duration.

3. Long-running imports must refresh/heartbeat their running-session lease periodically.

4. When creating a new session, use PostgreSQL CURRENT_TIMESTAMP/database time.

5. Atomically expire only genuinely stale uploading/running sessions.

6. Never expire a genuinely active running import merely because it has been running for a long time.

7. A Vercel/Render instance dying must not permanently block the user's next sync.

8. Preserve the existing per-user advisory lock and partial unique index.

9. Preserve Firebase authorization and ID-token verification.

10. Preserve all existing inventory data.

11. Preserve successful import behavior.

12. Preserve failed/cancelled/expired session cleanup.

13. If settings.sync:current_job is also capable of becoming stale, make its recovery logic lease/heartbeat based as well, but ONLY if the investigation confirms it is relevant to the production sync flow.

14. Do not introduce a fire-and-forget serverless workflow that depends on the process surviving after the HTTP response. If the current architecture has this problem, make the smallest safe change compatible with the existing Render API architecture.

IMPORTANT:
Do not blindly change values or delete locks just to make the error disappear.
The fix must distinguish an actually running import from an abandoned/stale session.

==================================================
PHASE 3 — VERIFICATION
==================================================

After implementing the fix:

1. Run workspace TypeScript checks.
2. Run API TypeScript checks.
3. Run API production build.
4. Run storefront/admin production builds as applicable.
5. Verify the API starts successfully.
6. Verify the sync session creation route.
7. Verify an unexpired active session is still rejected.
8. Verify an expired uploading session can be recovered.
9. Verify a running session's heartbeat extends its lease.
10. Verify terminal success/failure/cancellation still releases the session.

Do NOT modify production database data manually.

==================================================
FINAL REPORT
==================================================

Report clearly:

A. Investigation findings:
- Replit database vs Render database
- Firebase project comparison
- Exact source of the production 409
- Whether session 69b93615-860c-4b16-a35f-25b52379d039 is stale/active if determinable

B. Code changes:
- Files changed
- Session lease values
- Uploading lease duration
- Running lease duration
- Heartbeat interval
- Expiry/recovery logic
- Any sync:current_job changes

C. Verification:
- TypeScript result
- Production build result
- API health result
- Sync-lock tests

D. Deployment:
Tell me exactly what needs to be redeployed:
- Render API
- Vercel frontend
- Both
- Or neither

Do not deploy anything automatically unless the environment explicitly requires it.