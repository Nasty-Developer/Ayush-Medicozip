We now have two separate but related inventory-sync problems, and I want you to diagnose and fix BOTH carefully.

IMPORTANT:
Do not blindly delete database records.
Do not manually clear sync locks.
Do not create fake inventory data.
Do not change Firebase.
Do not change the medicine search UI unless the diagnosis proves it is necessary.
Do not modify duplicate frontend folders.
Do not deploy anything until all verification passes.

CURRENT PRODUCTION RESULT:

The latest Vercel production deployment is reaching Render correctly.

POST /api/sync/session currently returns:

{
  "error": "A sync is already in progress for this user.",
  "code": "sync_session_already_active",
  "sessionId": "...",
  "status": "running",
  "retryAt": "..."
}

The frontend correctly displays the active-sync/recovery UI.

This proves the old Vercel -> Render 405 routing problem is no longer the primary problem.

However, the important new observation is:

I performed an inventory sync and the operation appeared to work without showing an error, BUT afterwards medicines were not visible in the medicine search.

When I tried the sync again, I received the active-sync 409.

CURRENT REPLIT PREVIEW RESULT:

In Replit Preview, the Inventory Sync page now shows:

"Failed to create sync upload session."

So Preview is also currently unable to create the upload session.

I need you to investigate BOTH environments separately.

==================================================
PART 1 — PRODUCTION INVENTORY SYNC
==================================================

Trace the complete production flow:

Frontend
→ POST /api/sync/session
→ chunk uploads
→ POST /api/sync/start
→ persisted sync job
→ importer
→ PostgreSQL
→ medicine search API/data source

Determine exactly what happened to the first sync.

Specifically verify, using ONLY verified production evidence:

1. What is currently stored in:

settings.key = 'sync:current_job'

2. Inspect:

status
sessionId
firebaseUid
ownerId
startedAt
updatedAt
heartbeatAt
leaseExpiresAt

3. Determine whether the persisted job lease is:

- genuinely active
- expired/stale
- failed
- completed

4. Find the corresponding sync_sessions record.

5. Determine its current status.

6. Determine whether the first inventory sync actually reached the importer.

7. Determine whether the importer successfully processed:

PRODUCT.SDF
STOCK.SDF
COMPANY.SDF
CATEGORY.SDF
DRUG.SDF

8. Determine whether medicine/product records were actually written to the PRODUCTION PostgreSQL database.

9. Determine the relevant production row counts before/after the sync if reliable historical information exists.

10. Determine whether the customer medicine search reads from the exact same production tables/data that the importer writes.

11. Check Render production logs for errors/warnings associated with the first sync session.

12. Check whether the importer stopped early, timed out, failed, or is still running.

IMPORTANT:
Do NOT substitute the Replit development database for the production database.

If production PostgreSQL cannot be accessed safely/read-only from this environment, explicitly say that and tell me exactly what evidence is missing.

==================================================
PART 2 — PRODUCTION 409
==================================================

The current production 409 is:

sync_session_already_active
status: running

Do NOT automatically cancel it.

Determine whether it represents the same first sync or a different sync.

If the job/session is genuinely active, explain why the medicine data is not visible yet.

If the job is stale, implement the smallest safe lease-aware recovery mechanism.

The existing protections MUST remain:

- authentication
- ownership checks
- PostgreSQL advisory locks
- active-session protection
- 15-minute abandoned upload lease
- 2-hour running-session lease
- heartbeat
- persisted job lease

Do not weaken concurrency protection simply to make the error disappear.

==================================================
PART 3 — REPLIT PREVIEW
==================================================

Now independently trace why Replit Preview shows:

"Failed to create sync upload session."

Inspect the actual browser/network response for:

POST /api/sync/session

Determine:

- HTTP status
- response JSON
- whether authentication is present
- whether the Preview API is pointing to the correct backend
- whether Preview is accidentally using a different database
- whether Preview is using a stale API server process
- whether Preview is hitting the same Render backend or a local development API
- whether the frontend environment variables are correct
- whether the API route exists and is reachable
- whether the Preview error is caused by the same active-session protection or by a different problem

Do not assume Preview and Vercel production use the same backend configuration.

==================================================
PART 4 — MEDICINE VISIBILITY
==================================================

This is critical.

The sync appearing successful but medicines not appearing means we must verify the entire data path.

Trace:

SDF upload
→ parsing
→ import
→ PostgreSQL write
→ medicine API/query
→ frontend medicine search

Find exactly where the data disappears.

Do NOT assume the sync succeeded just because the frontend stopped showing an error.

If the database contains the imported medicines but the customer search cannot see them, identify the exact query/API mismatch.

If the database does not contain them, identify the exact importer failure.

If the importer is still running, prove it from production evidence.

==================================================
PART 5 — FIX
==================================================

After the diagnosis, implement the MINIMUM safe fix required for the actual root cause.

The fix must work in:

1. Replit Preview
2. Vercel production

Do not create a Preview-only workaround.

Do not create a Vercel-only workaround.

Do not bypass authentication.

Do not bypass sync concurrency protection.

Do not delete production data.

Do not reset the entire database.

Do not remove advisory locking.

Do not remove lease protection.

If code changes are required, modify the canonical production frontend:

artifacts/ayush-medico

and the production API:

artifacts/api-server

Only modify other files if the diagnosis proves they are required.

Do not modify:

artifacts/ayush-medico-app
artifacts/ayush-medico-storefront

==================================================
PART 6 — VERIFICATION
==================================================

Before reporting completion, run:

- API TypeScript check
- Frontend TypeScript check
- API production build
- Frontend production build
- focused sync lifecycle/concurrency tests
- git diff --check

Also verify:

- POST /api/sync/session behavior
- POST /api/sync/start behavior
- cancellation behavior
- stale-session behavior
- active-running behavior
- medicine data visibility/query path

For Replit Preview, verify the actual network response instead of relying only on UI text.

For production, do not claim the issue is fixed unless the relevant production endpoint/data path has been verified.

==================================================
FINAL REPORT
==================================================

At the end, clearly report:

A. Production sync status
B. Production 409 root cause
C. Replit Preview root cause
D. Whether the first sync actually imported medicines
E. Why medicines were/weren't visible
F. Exact files changed
G. Exact commits created
H. Verification results
I. Whether deployment is still required

Do NOT deploy automatically.

Do NOT modify the production database.

Do NOT guess.

The goal is not merely to make the 409 disappear.

The goal is to make the complete inventory pipeline reliable:

Upload
→ Sync session
→ Import
→ PostgreSQL
→ Medicine search
→ Customer-visible medicines

Fix the real root cause.