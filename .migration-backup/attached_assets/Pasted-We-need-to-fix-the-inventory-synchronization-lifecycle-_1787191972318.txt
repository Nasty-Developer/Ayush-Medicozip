We need to fix the inventory synchronization lifecycle completely.

IMPORTANT CONTEXT:

This is the first inventory sync I have intentionally attempted on the Vercel production website.

The production UI currently says:

"A sync is already in progress for this user."
Status: running
Retry after: 9:14:13 AM

I have NOT intentionally started a previous production inventory sync.

Also, our Replit Preview/development environment has already successfully completed an inventory import and imported approximately:
- 51,232 medicines
- 2,251 companies
- 14 categories
- 4,167 drug groups
- 820 stock records

So Preview works.

The problem is specifically the production Vercel/Render environment.

DO NOT assume that the production sync already completed.
DO NOT assume the production database contains the Preview data.
DO NOT copy Preview data into production.
DO NOT manually delete production database records.

FIRST perform a READ-ONLY investigation of the actual production environment.

Determine:

1. Which production sync_sessions row is causing:
   code: "sync_session_already_active"

2. Inspect:
   - sessionId
   - firebaseUid / owner
   - status
   - created_at
   - updated_at
   - heartbeat_at
   - expires_at

3. Inspect the production settings record:
   settings.key = "sync:current_job"

4. Inspect:
   - job status
   - job sessionId
   - startedAt
   - updatedAt
   - heartbeatAt
   - leaseExpiresAt

5. Determine whether the associated importer process is genuinely alive.

6. Determine whether the production sync is:
   A. genuinely running
   B. stale/abandoned
   C. already completed but incorrectly left as active

Do NOT modify anything during this diagnosis.

--------------------------------------------------
REQUIRED SYNC BEHAVIOR
--------------------------------------------------

After diagnosis, the implementation must enforce this behavior:

CASE 1 — Previous sync completed successfully

A completed sync must NEVER prevent another future sync.

Example:

Sync #1 → completed
Sync #2 → user clicks Sync tomorrow → MUST be accepted

The existence of old completed sync records must not cause HTTP 409.

--------------------------------------------------

CASE 2 — Previous sync failed/abandoned

A stale or abandoned sync must not permanently block future syncs.

If its importer/job lease has expired:

- safely mark the old session/job as expired/recovered/terminal
- preserve audit/history records
- do not delete records unnecessarily
- allow exactly one new sync to claim the lock

Use the existing PostgreSQL locking and lease mechanisms.

Do not simply remove the locking system.

--------------------------------------------------

CASE 3 — A sync is genuinely running right now

If an actual importer process is alive and its lease/heartbeat is valid:

- do NOT start a second simultaneous importer
- return the existing active-sync information
- show the user that a sync is genuinely running
- provide the existing safe cancellation mechanism if appropriate

This is the ONLY situation where an active-sync 409 should block a new sync.

--------------------------------------------------

CASE 4 — Old sync record says "running" but importer is dead

This is the situation we especially need to handle.

If:

sync_sessions.status = running

but:

- importer is no longer alive
- heartbeat is stale
- leaseExpiresAt has expired
- persisted job lease has expired

then the system must recognize the session as stale.

It must NOT continue telling the user:

"A sync is already running"

indefinitely.

Recover the stale state safely and allow a new sync.

--------------------------------------------------

CASE 5 — User intentionally syncs again after a successful import

This MUST work.

The inventory synchronization feature should be reusable.

A future workflow should be:

First sync:
SDF files → upload → import → completed

Later sync:
new SDF files → upload → import → completed

Later again:
updated SDF files → upload → import → completed

Previous successful imports must not permanently lock the system.

The importer should use the existing intended database semantics for updating/upserting inventory rather than creating duplicate medicine records.

--------------------------------------------------

VERY IMPORTANT DATABASE RULE

Do not solve this by deleting all sync_sessions.

Do not solve this by deleting settings.sync:current_job blindly.

Do not disable PostgreSQL advisory locking.

Do not remove the unique active-session protection.

Do not allow two real importers to run simultaneously.

Do not reset the production database.

Do not copy the development database into production.

Preserve:

- authentication
- ownership checks
- PostgreSQL advisory locks
- active-session uniqueness
- upload-session lease
- running-session lease
- persisted-job lease
- heartbeat mechanism
- audit/history information

--------------------------------------------------
PRODUCTION MEDICINE VISIBILITY

There is a second important issue.

After a successful production sync, the medicines must actually become visible through the production medicine/search API.

Do not assume that because the upload succeeds, the medicine catalog is populated.

Verify the complete production flow:

SDF upload
→ sync session
→ chunk upload
→ sync start
→ importer
→ PostgreSQL inventory/catalog tables
→ medicine API
→ Vercel customer medicine search

If the sync reports success but the medicine API still returns zero medicines, investigate that separately.

Do not use Preview's development database as proof that production has medicines.

--------------------------------------------------
VERIFICATION REQUIREMENTS

After making the minimal safe code changes:

1. Run API TypeScript check.
2. Run frontend TypeScript check.
3. Run API production build.
4. Run frontend production build.
5. Run focused sync lifecycle/concurrency tests.
6. Verify a completed sync does not block a new sync.
7. Verify an expired/stale sync can be recovered.
8. Verify a genuinely running sync still prevents concurrent imports.
9. Verify repeated future syncs are supported.
10. Verify medicine records become visible through the production medicine API after a successful production import.

Do not deploy until the code/build checks pass.

Do not modify production database records during testing unless explicitly required and clearly reported.

--------------------------------------------------
FINAL REPORT

At the end tell me clearly:

1. What production record is currently causing the 409.
2. Whether it is genuinely running or stale.
3. What code was changed.
4. Whether the fix allows repeated future syncs.
5. Whether concurrent real syncs are still protected.
6. Whether production medicine visibility was verified.
7. TypeScript result.
8. Build result.
9. Tests result.
10. Whether deployment is still required.

DO NOT make unrelated changes.
DO NOT redesign the inventory system.
DO NOT modify Firebase, customer UI, orders, payments, or unrelated features.