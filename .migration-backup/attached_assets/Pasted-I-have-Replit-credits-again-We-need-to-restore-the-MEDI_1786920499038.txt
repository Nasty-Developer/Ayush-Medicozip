I have Replit credits again. We need to restore the MEDICINE/SDF SYNC in Replit Preview FIRST.

IMPORTANT:
Do NOT work on Vercel yet.
Do NOT change deployment configuration yet.
Do NOT delete any more files.

CURRENT ISSUE:

The Ayush Medico project previously had a working medicine/SDF inventory sync in Replit Preview.

The admin uploads the medicine SDF file from the Inventory Sync page.

The frontend calls:

POST /api/sync/session

and then:

POST /api/sync/chunk
POST /api/sync/start
GET /api/sync/status

Previously this worked in Replit Preview.

Now Replit Preview returns:

401 Unauthorized

for:

POST /api/sync/session

IMPORTANT:
A 401 means the request is reaching an API/authentication layer and being rejected. Do NOT treat this as a 404 routing problem.

GOAL:

Restore the SDF medicine sync in Replit Preview exactly as it worked before.

DO NOT rebuild the medicine sync system.
DO NOT create a new upload system.
DO NOT change the database schema.
DO NOT change Firebase rules.
DO NOT change Cloudinary.
DO NOT change the Admin UI design.
DO NOT change the medicine dataset/parser.
DO NOT change the Vercel configuration yet.

FIRST: DIAGNOSE ONLY.

Trace the complete request:

Admin Inventory Sync page
→ SDF upload handler
→ /api/sync/session
→ Express server
→ authentication middleware
→ sync/session route

Inspect these files/configurations:

1. InventorySyncPage.tsx
2. The frontend API/client utility used by InventorySyncPage
3. artifacts/api-server/src/app.ts
4. artifacts/api-server/src/index.ts
5. The /api/sync/session route
6. Authentication middleware protecting /api/sync/*
7. Firebase authentication/token verification code
8. Replit/Vite development proxy configuration
9. Environment variables required for Firebase/admin authentication
10. api/[...path].ts
11. Any recent changes to api/index.ts

IMPORTANT RECENT CHANGE:

The repository originally contained:

api/index.ts
api/[...path].ts

Both exported the same Express app.

api/index.ts was recently deleted because it appeared to be a duplicate Vercel entrypoint.

DO NOT recreate it automatically.

Determine whether deleting api/index.ts could have affected Replit Preview routing.

IMPORTANT:

The current api/[...path].ts should remain unless your diagnosis proves it is causing the Replit Preview issue.

The current api/tsconfig.json contains:

{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    "noEmit": false
  },
  "include": ["."]
}

Do not change this unless your diagnosis proves it is relevant to the Replit 401.

DIAGNOSE THE 401.

Determine exactly which one is happening:

A. Authorization header is missing
B. Authorization header is malformed
C. Firebase ID token is expired/invalid
D. Firebase token is valid but admin authorization fails
E. Wrong Firebase environment variable/project
F. Replit Preview is sending the request to the wrong backend
G. Deleting api/index.ts changed routing
H. Another specific cause

You may add temporary SAFE diagnostic logging if necessary.

NEVER log:
- Firebase ID tokens
- passwords
- session secrets
- API keys
- private credentials

Safe diagnostics may report:
- request method
- request path
- whether Authorization header exists
- whether it begins with "Bearer "
- which authentication step succeeds/fails

Do NOT modify production behavior just to hide the 401.

After diagnosis:

1. Tell me the exact root cause.
2. Tell me the minimum change needed.
3. Make ONLY that minimum change.
4. Run the relevant checks.
5. Start Replit Preview.
6. Test the SDF sync endpoint.
7. Confirm whether POST /api/sync/session now reaches the correct route.
8. If possible, test the complete SDF upload flow through the Admin Inventory Sync page.

SUCCESS CRITERIA:

Admin logs in
→ opens Inventory Sync
→ selects SDF file
→ uploads/syncs
→ /api/sync/session succeeds
→ chunks upload
→ sync starts
→ medicine records are imported
→ no 401/404

DO NOT move on to Vercel until this works in Replit Preview again.

After fixing it, report:
- exact root cause
- files changed
- why the fix works
- confirmation that Replit Preview SDF sync works again

Only after I approve the result should we return to the Vercel deployment problem.