We now have production logs proving the actual failure. Do not guess and do not modify the database manually.

Production sync successfully creates the session, uploads all five SDF files successfully, and /api/sync/start returns HTTP 202. Immediately afterward, the Render Node process crashes with:

FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory

The production log therefore proves the importer is exhausting the Node.js heap during the PostgreSQL import.

Fix the inventory importer so it can process the complete SDF dataset without exhausting Render's Node.js memory.

First inspect the actual importer implementation and identify what data is retained in memory during import. Refactor it to use streaming/incremental parsing and bounded batches where appropriate. Do not load the complete PRODUCT.SDF/catalog into memory. Release processed data between batches and avoid accumulating large arrays/maps unnecessarily.

Increasing --max-old-space-size may be used only if appropriate as a secondary safeguard; it must NOT replace fixing unbounded memory usage.

Also fix the sync-session recovery for a process crash. If the importer dies after /api/sync/start has successfully returned 202, the associated running sync session must eventually become recoverable based on the importer/job heartbeat/lease rather than remaining blocked for the full two-hour running lease. A healthy importer must continue renewing its lease normally. Do not weaken authentication, PostgreSQL advisory locks, ownership checks, or single-import protection.

Do not manually delete production rows.

Do not disable concurrency protection.

Do not create fake data.

Do not change the Vercel frontend unless required.

Verify with the complete five-file SDF import path, TypeScript check, production build, focused sync/concurrency tests, and memory/lifecycle assertions.

At the end, clearly report:

exact importer code causing excessive memory usage,
exact fix,
how memory is now bounded,
how a crashed importer releases/recoverably expires its session,
verification results.

Do not deploy anything until the code fix and verification are complete.