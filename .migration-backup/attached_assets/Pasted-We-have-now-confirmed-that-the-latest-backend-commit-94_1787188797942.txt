We have now confirmed that the latest backend commit 940637e is deployed and LIVE on Render.

Do NOT modify Vercel configuration.
Do NOT modify the Vercel Root Directory.
Do NOT create another Vercel project.
Do NOT modify Firebase.
Do NOT delete any database records manually.
Do NOT change the duplicate frontend folders.

Current production flow is reaching Render successfully.

The deployed frontend currently receives this response from POST /api/sync/start:

{
  "error": "A sync is already running on another instance.",
  "code": "already_running"
}

This is now the ONLY issue we need to investigate.

Please inspect the current inventory synchronization lifecycle in:
- artifacts/api-server/src/routes/sync.ts
- all sync/session/job/lease/lock related backend code
- the PostgreSQL schema/migrations used by sync
- the frontend InventorySyncPage.tsx

Determine exactly what `code: "already_running"` means and which database record / job / advisory lock is causing it.

IMPORTANT:
1. Do not guess.
2. Do not delete database records.
3. Do not disable the concurrency protection.
4. Do not remove PostgreSQL locking.
5. Do not weaken authentication or ownership checks.
6. Do not automatically cancel another user's genuinely active sync.
7. Do not change Vercel routing.

We need a SAFE recovery mechanism for an abandoned/stale sync while preserving protection against two real simultaneous syncs.

First perform a READ-ONLY diagnosis of:
- the active session/job detection logic
- the exact condition producing `already_running`
- whether the existing active state belongs to the current authenticated user
- whether it is genuinely running or stale
- how its heartbeat/updated_at/lease is determined
- whether the 15-minute abandoned-upload lease applies to this state
- whether the 2-hour running lease applies
- whether an advisory lock is currently held
- whether the existing cancellation endpoint can safely cancel the specific stale job/session

Also compare this with the frontend lifecycle changes from commit 940637e.

DO NOT modify files yet.

At the end, report:
A. Exact source of `already_running`
B. Exact database/job/session state responsible
C. Whether it is stale or genuinely active
D. Safe recovery method
E. Whether any code change is actually required

STOP after the diagnosis. Do not implement anything until the diagnosis is reviewed.