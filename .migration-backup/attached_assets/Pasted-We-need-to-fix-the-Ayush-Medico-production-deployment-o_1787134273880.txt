We need to fix the Ayush Medico production deployment on Vercel.

IMPORTANT: DO NOT DELETE, MOVE, RENAME, OR MODIFY the duplicate frontend folders yet.

Do NOT delete:
- artifacts/ayush-medico-app
- artifacts/ayush-medico-storefront

We will clean those up only AFTER the production deployment is working.

The canonical production frontend is:

artifacts/ayush-medico

The Vercel project currently has:

Root Directory:
artifacts/ayush-medico

The Vite build is succeeding, but Vercel Production deployment is failing after the build.

Current production architecture:
Vercel frontend:
artifacts/ayush-medico

Backend:
https://ayush-medicozip-1.onrender.com

The Render backend health endpoint is working:
https://ayush-medicozip-1.onrender.com/api/healthz

It returns HTTP 200.

The production frontend API route currently fails because:
POST https://ayush-medico.vercel.app/api/sync/session
returns HTTP 405 and falls through to index.html instead of reaching Render.

We need to fix BOTH deployment output configuration and API routing.

IMPORTANT VERCEL ROOT DIRECTORY RULE:

Because Vercel Root Directory is:

artifacts/ayush-medico

all paths inside the Vercel project configuration must be considered relative to that directory.

The frontend build output is:

dist/public

NOT:

artifacts/ayush-medico/dist/public

Inspect the existing Vite configuration and confirm the actual build output before changing anything.

TASK 1 — Inspect current configuration

Inspect:

- artifacts/ayush-medico/vercel.json
- root vercel.json
- artifacts/ayush-medico/package.json
- root package.json
- pnpm-workspace.yaml
- pnpm-lock.yaml
- Vite configuration
- any existing api/[...path].ts or API routing files

Determine exactly which configuration Vercel is actually using when Root Directory is set to:

artifacts/ayush-medico

Do not guess.

TASK 2 — Fix Vercel build output

The Vercel project must continue using:

Root Directory:
artifacts/ayush-medico

Do NOT change the Root Directory.

The final Vercel configuration must correctly identify the production build output as:

dist/public

Make the minimum required repository configuration changes so Vercel can collect and serve the generated Vite output.

Do not introduce a second frontend deployment.

Do not create another Vercel project.

TASK 3 — Preserve monorepo dependency support

The canonical frontend depends on:

@workspace/api-client-react

which is part of the monorepo.

Make sure the Vercel build remains reproducible with the existing pnpm workspace and frozen lockfile.

Do not break the workspace dependency resolution.

Do not delete or regenerate pnpm-lock.yaml unnecessarily.

Do not use npm/yarn instead of the existing pnpm setup.

TASK 4 — Fix production /api routing

The intended architecture is:

Browser
  ↓
Vercel frontend
  ↓ /api/*
Render backend
  ↓
https://ayush-medicozip-1.onrender.com

Configure the canonical Vercel deployment so:

/api/*

is forwarded to:

https://ayush-medicozip-1.onrender.com/api/*

Do not create a separate API Vercel deployment.

Do not change the Render backend.

Do not remove authentication from the backend.

Do not expose credentials.

TASK 5 — SPA routing

The application uses React client-side routing.

Direct production navigation must work for:

/
/admin
/admin/login
/medicines

without Vercel returning NOT_FOUND merely because the browser directly requests the route.

Implement the minimum appropriate SPA fallback.

IMPORTANT:
The /api/* route must be handled BEFORE the SPA fallback so API requests do not become index.html responses.

TASK 6 — Do NOT modify application functionality

Do not redesign the website.

Do not modify Firebase functionality.

Do not modify medicine logic.

Do not modify orders.

Do not modify admin functionality unless a change is strictly required for deployment routing.

Do not modify the database.

Do not modify Render.

This task is deployment configuration only.

TASK 7 — Verify before finishing

Run:

1. pnpm install --frozen-lockfile
2. the existing production build
3. TypeScript checks

Confirm the build creates:

artifacts/ayush-medico/dist/public

and, from the Vercel Root Directory, this corresponds to:

dist/public

Then inspect the final configuration.

TASK 8 — DO NOT CLEAN DUPLICATES YET

Do NOT remove:

artifacts/ayush-medico-app
artifacts/ayush-medico-storefront

Do not modify their workflows or artifact registrations.

Do not modify their lockfile importers.

We will perform a separate cleanup after Production deployment is confirmed working.

TASK 9 — FINAL REPORT

At the end, report exactly:

- files modified
- final Vercel Root Directory
- final Build Command
- final Output Directory
- final API routing configuration
- final SPA fallback configuration
- build result
- TypeScript result
- whether any duplicate folders were deleted (answer must be NO)
- any remaining deployment issue

STOP after this deployment-fix task.